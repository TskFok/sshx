# CSP 防护 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Tauri WebView 上启用生产强制 CSP，并用独立 `devCsp` 保住 `tauri dev` 的 Vite HMR。

**Architecture:** 唯一来源是 `src-tauri/tauri.conf.json` 的 `app.security.csp` 与 `app.security.devCsp`（对象格式完整策略，开发不是增量合并）。不改 `index.html`、不增加 meta、不抽策略模块。Vitest 读取该 JSON，锁定生产脚本源不得放宽、开发必须包含本机 HMR 源。`assetProtocol` 保持现状。

**Tech Stack:** Tauri 2、Vite 8、Vitest 4、React 18

## Global Constraints

- 在当前分支修改，不新建分支。
- 只改 `src-tauri/tauri.conf.json` 的 `app.security` 并新增 `src/lib/tauriCsp.test.ts`。
- `csp` 与 `devCsp` 使用对象格式；`devCsp` 是完整策略，不是在 `csp` 上做增量合并。
- 生产 `script-src` 仅为 `'self'`，不含 `'unsafe-eval'`、`'unsafe-inline'`，也不含 `http:` / `https:` 远程源。
- 生产 `style-src` 必须含 `'unsafe-inline'` 与 `'self'`。
- 不修改 `index.html`，不增加 CSP meta，不抽出策略生成模块。
- `assetProtocol` 保持 `enable: true`、`scope: ["**"]`。
- 不为 WASM 增加 `'wasm-unsafe-eval'`，不为 `TAURI_DEV_HOST` 局域网地址放行。
- 提交信息使用 Conventional Commits 英文前缀和简体中文说明。
- 先写失败测试，再改配置。

---

### Task 1: Tauri CSP 配置与契约测试

**Files:**
- Create: `src/lib/tauriCsp.test.ts`
- Modify: `src-tauri/tauri.conf.json`（`app.security`，当前 `csp` 为 `null`，无 `devCsp`）

**Interfaces:**
- Consumes: Node `readFileSync` 与 `import.meta.url` 读取 `src-tauri/tauri.conf.json`；不修改 Vitest 配置。
- Produces: `app.security.csp` 与 `app.security.devCsp` 两个完整指令对象；不新增 TypeScript 运行时 API。

- [x] **Step 1: 写入失败的 CSP 契约测试**

Create `src/lib/tauriCsp.test.ts`:

```ts
import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";

type CspMap = Record<string, string>;

const tauriConfig = JSON.parse(
  readFileSync(new URL("../../src-tauri/tauri.conf.json", import.meta.url), "utf8")
) as {
  app: {
    security: {
      csp: CspMap | null;
      devCsp?: CspMap | null;
    };
  };
};

function assertDirectiveMap(value: unknown, label: string): CspMap {
  expect(value, `${label} 必须是对象`).toBeTruthy();
  expect(value, label).toBeTypeOf("object");
  expect(Array.isArray(value), `${label} 不能是数组`).toBe(false);
  return value as CspMap;
}

function tokens(directive: string | undefined, label: string): string[] {
  if (typeof directive !== "string") {
    throw new Error(`${label} 必须是字符串`);
  }
  return directive.split(/\s+/).filter(Boolean);
}

describe("tauri CSP", () => {
  const security = tauriConfig.app.security;
  const csp = assertDirectiveMap(security.csp, "app.security.csp");
  const devCsp = assertDirectiveMap(security.devCsp, "app.security.devCsp");
  const scriptSrc = tokens(csp["script-src"], "csp.script-src");
  const styleSrc = tokens(csp["style-src"], "csp.style-src");
  const imgSrc = tokens(csp["img-src"], "csp.img-src");
  const connectSrc = tokens(csp["connect-src"], "csp.connect-src");
  const devScriptSrc = tokens(devCsp["script-src"], "devCsp.script-src");
  const devConnectSrc = tokens(devCsp["connect-src"], "devCsp.connect-src");

  it("csp 与 devCsp 均为对象且不为 null", () => {
    expect(csp).toEqual(expect.any(Object));
    expect(devCsp).toEqual(expect.any(Object));
  });

  it("生产 script-src 仅为本地脚本，不含 eval、内联脚本或远程 http(s)", () => {
    expect(scriptSrc).toContain("'self'");
    expect(scriptSrc).not.toContain("'unsafe-eval'");
    expect(scriptSrc).not.toContain("'unsafe-inline'");
    expect(
      scriptSrc.filter((token) => token.startsWith("http:") || token.startsWith("https:"))
    ).toEqual([]);
  });

  it("生产 style-src 允许内联样式与本地样式", () => {
    expect(styleSrc).toContain("'unsafe-inline'");
    expect(styleSrc).toContain("'self'");
  });

  it("生产 img-src 覆盖 asset 协议与 Windows asset.localhost", () => {
    expect(imgSrc).toContain("asset:");
    expect(imgSrc).toContain("http://asset.localhost");
    expect(imgSrc).toContain("https://asset.localhost");
  });

  it("生产 connect-src 仅服务 Tauri IPC", () => {
    expect(connectSrc).toContain("ipc:");
    expect(connectSrc).toContain("http://ipc.localhost");
  });

  it("生产禁止插件对象与 iframe", () => {
    expect(csp["object-src"]).toBe("'none'");
    expect(csp["frame-src"]).toBe("'none'");
  });

  it("devCsp.script-src 允许 Vite HMR 所需的 eval 与本机开发源", () => {
    expect(devScriptSrc).toContain("'unsafe-eval'");
    expect(devScriptSrc).toContain("http://localhost:1420");
  });

  it("devCsp.connect-src 允许本机 HTTP 与 WebSocket HMR", () => {
    expect(devConnectSrc).toContain("ws://localhost:1420");
    expect(devConnectSrc).toContain("ws://localhost:1421");
  });

  it("生产与开发策略等于设计文档中的完整指令表", () => {
    expect(csp).toEqual({
      "default-src": "'self' customprotocol: asset:",
      "script-src": "'self'",
      "style-src": "'unsafe-inline' 'self'",
      "img-src":
        "'self' asset: http://asset.localhost https://asset.localhost blob: data:",
      "connect-src": "ipc: http://ipc.localhost https://ipc.localhost",
      "font-src": "'self' data:",
      "object-src": "'none'",
      "base-uri": "'self'",
      "form-action": "'self'",
      "frame-src": "'none'",
    });
    expect(devCsp).toEqual({
      "default-src": "'self' customprotocol: asset: http://localhost:1420",
      "script-src": "'self' 'unsafe-inline' 'unsafe-eval' http://localhost:1420",
      "style-src": "'unsafe-inline' 'self' http://localhost:1420",
      "img-src":
        "'self' asset: http://asset.localhost https://asset.localhost blob: data: http://localhost:1420",
      "connect-src":
        "ipc: http://ipc.localhost https://ipc.localhost http://localhost:1420 ws://localhost:1420 ws://localhost:1421",
      "font-src": "'self' data: http://localhost:1420",
      "object-src": "'none'",
      "base-uri": "'self'",
      "form-action": "'self'",
      "frame-src": "'none'",
    });
  });
});
```

- [x] **Step 2: 运行测试并确认失败**

Run: `pnpm exec vitest run src/lib/tauriCsp.test.ts`

Expected: FAIL。`app.security.csp` 当前为 `null`，且没有 `devCsp`，`assertDirectiveMap` 在 `csp 与 devCsp 均为对象且不为 null` 之前就会因 `app.security.csp 必须是对象` 失败。不要在这一步修改 `tauri.conf.json`。

- [x] **Step 3: 写入生产 CSP 与开发 CSP**

将 `src-tauri/tauri.conf.json` 的 `app.security` 替换为：

```json
    "security": {
      "csp": {
        "default-src": "'self' customprotocol: asset:",
        "script-src": "'self'",
        "style-src": "'unsafe-inline' 'self'",
        "img-src": "'self' asset: http://asset.localhost https://asset.localhost blob: data:",
        "connect-src": "ipc: http://ipc.localhost https://ipc.localhost",
        "font-src": "'self' data:",
        "object-src": "'none'",
        "base-uri": "'self'",
        "form-action": "'self'",
        "frame-src": "'none'"
      },
      "devCsp": {
        "default-src": "'self' customprotocol: asset: http://localhost:1420",
        "script-src": "'self' 'unsafe-inline' 'unsafe-eval' http://localhost:1420",
        "style-src": "'unsafe-inline' 'self' http://localhost:1420",
        "img-src": "'self' asset: http://asset.localhost https://asset.localhost blob: data: http://localhost:1420",
        "connect-src": "ipc: http://ipc.localhost https://ipc.localhost http://localhost:1420 ws://localhost:1420 ws://localhost:1421",
        "font-src": "'self' data: http://localhost:1420",
        "object-src": "'none'",
        "base-uri": "'self'",
        "form-action": "'self'",
        "frame-src": "'none'"
      },
      "assetProtocol": {
        "enable": true,
        "scope": [
          "**"
        ]
      }
    }
```

不要改 `windows`、`bundle`、`build` 或其他字段。不要改 `index.html`。

- [x] **Step 4: 运行目标测试并确认通过**

Run: `pnpm exec vitest run src/lib/tauriCsp.test.ts`

Expected: PASS，`1` 个测试文件、`9` 个测试通过，输出中没有错误或警告。

- [x] **Step 5: 运行全量自动验证**

Run: `pnpm test`

Expected: PASS，全部 Vitest 测试通过。

Run: `pnpm build`

Expected: PASS，TypeScript 与 Vite 构建成功，输出中没有编译错误。

- [x] **Step 6: 人工验证 WebView 行为**

运行 `pnpm tauri dev`，确认：

1. 应用能启动，控制台没有 CSP 拒绝本地脚本或样式的报错。
2. Vite HMR 修改一段前端文案后能热更新。
3. 打开已有 SSH 连接或设置页：`invoke` 正常（IPC 未被拦）。
4. 若已配置动态壁纸，终端/设置预览中壁纸图片能显示（`asset:` 未被拦）。

- [x] **Step 7: 提交实现**

```bash
git add src/lib/tauriCsp.test.ts src-tauri/tauri.conf.json docs/superpowers/plans/2026-09-20-csp.md
git commit -m "feat: 开启 WebView CSP 防护"
```
